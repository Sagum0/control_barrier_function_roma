# 2026-09-04

## Summary
PLAN12를 실행해 `zed_fusion_fulllog.py`가 융합 처리에 성공한 매 프레임마다 하트비트
레코드를 기록하도록 변경했다. 사람이 없는 프레임도 `n_bodies: 0`, `body_ids: []`로 남으며,
기존 fused/raw/metrics 레코드와 CLI는 변경하지 않았다.

PLAN13도 실행해 fulllog 산출물을 실행별 `fulllog_<stamp>/` 디렉터리에 묶고, SIGTERM을
SIGINT와 같은 stop 플래그 핸들러에 연결했다. `--record-svo DIR` 동작은 그대로 유지했다.

PLAN14를 실행해 카메라별 MP4를 시스템 ffmpeg의 `libx264`로 직접 기록하도록 바꾸고,
ffmpeg가 없거나 `--video-mp4v`를 지정하면 기존 OpenCV mp4v를 사용하도록 했다.

## Changes
| 파일 | 변경 내용 |
|------|-----------|
| `scripts/zed_fusion_fulllog.py` | fused 레코드보다 먼저 `record:"frame"`을 기록하고 라이브·종료 요약에 `frame_records` 카운터 추가 |
| `docs/plans/2026_09/03_PLAN12_fulllog_frame_heartbeat.md` | 승인된 구현과 정적 검증 완료 후 `status: done`으로 변경 |
| `docs/worklog/2026-09-03.md` | PLAN12 완료 상태와 하트비트 변경 내역을 Changes 표에 반영 |
| `scripts/zed_fusion_fulllog.py`, `docs/plans/2026_09/04_PLAN13_fulllog_run_dir.md` | 실행별 하위 디렉터리·meta `run_dir`·SIGTERM 정상 종료를 구현하고 정적 검증 후 plan을 `done`으로 변경 |
| `scripts/zed_fusion_fulllog.py`, `docs/plans/2026_09/04_PLAN14_fulllog_h264_video.md` | ffmpeg `libx264` writer·mp4v 폴백·`--video-mp4v`·meta 코덱 기록을 추가하고 검증 후 plan을 `done`으로 변경 |
| `logs/README.md` | **신규** — fulllog 데이터 명세(디렉터리 구조, 4종 레코드 필드, BODY_18/34 관절 인덱스 표(SDK enum 추출), meta.json, 함정, 읽기 예시). 사용자 요청으로 타인 열람용 |

참조 plan: `docs/plans/2026_09/03_PLAN12_fulllog_frame_heartbeat.md`

## Verification
- [x] `python3 -m py_compile scripts/zed_fusion_fulllog.py`
- [x] `conda run -n zed python3 scripts/zed_fusion_fulllog.py --help` 정상 종료, 옵션 변화 없음
- [x] 빈 `body_list` 합성 frame 레코드가 `json.dumps(..., allow_nan=False)`를 통과하고
  `n_bodies: 0`, `body_ids: []`임을 확인
- [ ] 빈 방 5초와 사람 등장 5초 라이브 로그 확인 — 카메라·GPU가 있는 사용자 환경에서 수행
- [x] PLAN13: `python3 -m py_compile scripts/zed_fusion_fulllog.py`
- [x] PLAN13: `conda run -n zed python3 scripts/zed_fusion_fulllog.py --help` 정상 종료,
  옵션 집합 유지 및 `--outdir` 하위 디렉터리 설명 확인
- [x] PLAN13: `/tmp/fl_test/fulllog_<stamp>` 경로와 JSONL·meta·두 MP4의 부모 및 기존 파일명 확인
- [x] PLAN13: 실제 등록 블록에서 SIGINT와 SIGTERM이 같은 핸들러 객체를 사용하고 stop 플래그를
  설정함을 확인
- [ ] PLAN13: 실행별 파일 4개 생성과 `kill <pid>` 후 MP4 정상 종료 확인 — 카메라·GPU가 있는
  사용자 환경에서 수행
- [x] PLAN14: `python3 -m py_compile scripts/zed_fusion_fulllog.py`
- [x] PLAN14: `conda run -n zed python3 scripts/zed_fusion_fulllog.py --help` 정상 종료 및
  `--video-mp4v` 노출 확인
- [x] PLAN14: 1280x720 검은 프레임 90장을 ffmpeg writer로 기록한 뒤 `ffprobe`에서
  `codec_name=h264`, `nb_frames=90` 확인
- [x] PLAN14: `use_mp4v=True` writer로 같은 프레임 90장을 기록한 뒤 `ffprobe`에서
  `codec_name=mpeg4`, `nb_frames=90` 확인
- [x] PLAN14: `PATH=/usr/bin:/bin`에서 ffmpeg 미탐지 경고와 mp4v 폴백 writer open 확인
- [ ] PLAN14: 두 카메라 라이브 촬영의 프레임 수·`received_fps`·SIGTERM 후 재생 확인 —
  카메라·GPU가 있는 사용자 환경에서 수행

## Next steps
- 사용자 환경에서 라이브 촬영 후 매 성공 프레임의 frame 레코드 존재 여부와 body id 일치를 확인한다.
- 사용자 환경에서 `logs/fulllog_<stamp>/` 산출물과 SIGTERM 종료 후 MP4를 확인한다.
- 사용자 환경에서 기본 H.264 촬영의 프레임 수와 `received_fps`를 확인하고 VS Code에서 재생한다.
- frame 레코드 기준 30Hz 타임라인으로 velocity 지연·covariance 특성을 분석한다.

## 해결해야 할 점
- 없음. 라이브 하드웨어 검증만 남았다.
